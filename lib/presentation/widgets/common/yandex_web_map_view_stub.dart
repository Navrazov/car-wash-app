import 'package:flutter/widgets.dart';

class YandexWebMapView extends StatelessWidget {
  final String url;

  const YandexWebMapView({
    super.key,
    required this.url,
  });

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
